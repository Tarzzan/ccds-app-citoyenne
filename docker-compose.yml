# ─────────────────────────────────────────────────────────────
# Ma Commune — Environnement de développement Docker
# Usage : docker-compose up -d
# ─────────────────────────────────────────────────────────────

services:

  # ── Nginx (reverse proxy) ─────────────────────────────────
  nginx:
    image: nginx:1.25-alpine
    container_name: ma_commune_nginx
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ./backend:/var/www/backend:ro
      - ./admin:/var/www/admin:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./docker/nginx/ssl:/etc/nginx/ssl:ro
      - uploads_data:/var/www/backend/uploads
    depends_on:
      - php
    networks:
      - ma_commune_net
    restart: unless-stopped

  # ── PHP-FPM 8.4 ───────────────────────────────────────────
  php:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    container_name: ma_commune_php
    volumes:
      - ./backend:/var/www/backend
      - ./admin:/var/www/admin
      - ./tests:/var/www/tests:ro
      - uploads_data:/var/www/backend/uploads
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: ${DB_NAME:-ma_commune_db}
      DB_USER: ${DB_USER:-ma_commune_user}
      DB_PASS: ${DB_PASS:-ma_commune_pass}
      JWT_SECRET: ${JWT_SECRET:-change_me_in_production}
      APP_ENV: development
      APP_REFERENCE_PREFIX: MC
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - ma_commune_net
    restart: unless-stopped

  # ── MySQL 8.0 ─────────────────────────────────────────────
  mysql:
    image: mysql:8.0
    container_name: ma_commune_mysql
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpass}
      MYSQL_DATABASE: ${DB_NAME:-ma_commune_db}
      MYSQL_USER: ${DB_USER:-ma_commune_user}
      MYSQL_PASSWORD: ${DB_PASS:-ma_commune_pass}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docs/database.sql:/docker-entrypoint-initdb.d/01_schema.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ma_commune_net
    restart: unless-stopped

  # ── phpMyAdmin (interface BDD) ────────────────────────────
  phpmyadmin:
    image: phpmyadmin:5.2-apache
    container_name: ma_commune_phpmyadmin
    ports:
      - "8080:80"
    environment:
      PMA_HOST: mysql
      PMA_USER: ${DB_USER:-ma_commune_user}
      PMA_PASSWORD: ${DB_PASS:-ma_commune_pass}
    depends_on:
      - mysql
    networks:
      - ma_commune_net
    profiles:
      - tools

  # ── WebSocket Server (Ratchet) ────────────────────────────
  websocket:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    container_name: ma_commune_websocket
    command: ["php", "/var/www/backend/websocket/server.php"]
    ports:
      - "8081:8081"
    volumes:
      - ./backend:/var/www/backend
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: ${DB_NAME:-ma_commune_db}
      DB_USER: ${DB_USER:-ma_commune_user}
      DB_PASS: ${DB_PASS:-ma_commune_pass}
      JWT_SECRET: ${JWT_SECRET:-change_me_in_production}
      WS_PORT: 8081
      APP_REFERENCE_PREFIX: MC
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - ma_commune_net
    restart: unless-stopped

# ── Volumes persistants ───────────────────────────────────────
volumes:
  mysql_data:
    driver: local
  uploads_data:
    driver: local

# ── Réseau interne ────────────────────────────────────────────
networks:
  ma_commune_net:
    driver: bridge
